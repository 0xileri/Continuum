[pytest]
testpaths = tests
# tests/ is a plain directory rather than a package, so conftest helpers are imported by module
# name. rootdir-relative importmode keeps that working without an __init__.py that would otherwise
# exist only to satisfy the test runner.
addopts = -q --import-mode=prepend
filterwarnings =
    ignore::DeprecationWarning
